var target = 'input.EditForm__key-or-id__input';

function addLink () {
	// var slug = getElementsByClassName(target).val();
	// if (!slug) return setTimeout(addLink, 1000);
	// getElementsByClassName(target).after('<a title="Preview" href="/blog/post/' + slug + '" target="_blank">' + slug + ' (preview)</a>');
	// getElementsByClassName(target).remove();
}

addLink();
