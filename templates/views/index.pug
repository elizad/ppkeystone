extends ../layouts/default

block content
	.container: .text-center
		
		h1.text-center Straight-talking pension advice.
		p.text-center We find your pensions and tell you about them for free, with no obligation.
		a(href='/contact').btn.btn-lg.btn-primary Find Pension
		p It includes the latest versions of
			| <a href='http://getbootstrap.com/' target='_blank'>Bootstrap</a>
			| and <a href='http://www.jquery.com/' target='_blank'>jQuery</a>.
		p Visit 
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			p We have created a default Admin user for you with the email <strong>elizadascal@gmail.com</strong> and the password <strong>light</strong>.
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
		hr
		p
